<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kop.app"> 

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- START OF ADDED PERMISSION FOR AI FEATURE -->
    <!-- Required to allow the app to connect to the Gemini API over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- END OF ADDED PERMISSION FOR AI FEATURE -->

    <!-- Declare that the app uses the camera feature -->
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <!-- *** START OF ADDED FEATURE DECLARATION FOR GPU SUPPORT *** -->
    <!-- Declares that the app requires OpenGL ES 2.0 or higher for GPU rendering. -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <!-- *** END OF ADDED FEATURE DECLARATION FOR GPU SUPPORT *** -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/kop"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- *** START OF ADDED ACTIVITY DECLARATION FOR LIVE CAMERA *** -->
        <!-- Registers the new GPU-based camera activity with the Android system. -->
        <activity
            android:name=".CameraFilterActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />
        <!-- *** END OF ADDED ACTIVITY DECLARATION FOR LIVE CAMERA *** -->

        <!--
          REMOVED: The declarations for MediaPickerActivity and ProcessingActivity are gone
          because they have been converted into DialogFragments, which do not need to be
          declared in the manifest.
        -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

    </application>
</manifest>
