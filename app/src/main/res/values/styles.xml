<?xml version="1.0" encoding="utf-8"?>
<resources>  
    <!-- 
      FIX: The parent theme is now the base AppCompat theme (which is dark by default)
      instead of the specific Light theme. This will match the HTML UI.
    -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- You can customize your theme attributes here in the future if needed. -->
    </style>
</resources>
